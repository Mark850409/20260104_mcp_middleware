# Python 套件優化說明

## 🎯 問題分析

### 安裝緩慢的原因

**`sentence-transformers==2.2.2`** 是導致安裝緩慢的主要原因:

1. **自動下載大型模型**: 該套件會自動下載預訓練的深度學習模型
2. **模型大小**: 通常 300MB - 2GB 不等
3. **依賴項多**: 包含 PyTorch、transformers 等大型依賴

## ✅ 優化方案

### 1. Backend Requirements

已將 `sentence-transformers` 標記為**可選依賴**:

```txt
# ============================================
# RAG 相關 - 可選套件 (僅在使用本地 Embedding 時需要)
# ============================================
# 注意: sentence-transformers 會下載大型模型 (數百MB到數GB)
# 如果只使用雲端 Embedding API (OpenAI/Google),可以不安裝此套件
# 安裝方式: pip install sentence-transformers==2.2.2
# sentence-transformers==2.2.2
```

### 2. 使用場景

#### 場景 A: 僅使用雲端 API (推薦)
```bash
# 快速安裝,不含本地模型
pip install -r requirements.txt
```

**優點:**
- ✅ 安裝速度快 (約 30 秒)
- ✅ 不佔用本地儲存空間
- ✅ 使用 OpenAI/Google 的高品質 Embedding

**適用於:**
- 有 OpenAI API Key
- 有 Google API Key
- 不需要離線使用

#### 場景 B: 需要本地 Embedding
```bash
# 1. 先安裝基礎套件
pip install -r requirements.txt

# 2. 再安裝本地模型套件
pip install sentence-transformers==2.2.2
```

**優點:**
- ✅ 可離線使用
- ✅ 不需要 API Key
- ✅ 無 API 調用費用

**缺點:**
- ⚠️ 首次安裝慢 (約 5-10 分鐘)
- ⚠️ 佔用儲存空間 (約 1-2GB)

## 📊 安裝時間對比

| 方案 | 安裝時間 | 儲存空間 | API 需求 |
|------|---------|---------|---------|
| 僅雲端 API | ~30 秒 | ~100MB | 需要 |
| 含本地模型 | ~5-10 分鐘 | ~1-2GB | 不需要 |

## 🔧 RAG 服務配置

### 使用雲端 Embedding (預設)

在知識庫配置中選擇:
- **Provider**: `openai` 或 `google`
- **Model**: 
  - OpenAI: `text-embedding-3-small` (推薦)
  - Google: `embedding-001`

### 使用本地 Embedding

1. 先安裝 sentence-transformers:
   ```bash
   pip install sentence-transformers==2.2.2
   ```

2. 在知識庫配置中選擇:
   - **Provider**: `local`
   - **Model**: `paraphrase-multilingual-MiniLM-L12-v2` (預設)

## 🚀 建議

### 開發環境
- 使用雲端 API,安裝快速
- 設定環境變數:
  ```bash
  OPENAI_API_KEY=your_key_here
  # 或
  GOOGLE_API_KEY=your_key_here
  ```

### 生產環境
- 根據需求選擇:
  - **有網路**: 使用雲端 API (成本低,品質高)
  - **無網路**: 使用本地模型 (一次性安裝成本)

## 📝 其他優化

### MCP Server Requirements
已整理並加入分類說明,所有套件都是必要的:
- Web 框架: Flask, Flask-CORS
- MCP 相關: fastmcp, python-dotenv
- ASGI Server: starlette, uvicorn
- HTTP Client: httpx, httpx-sse
- 資料庫: pymysql

## 🎉 結果

優化後的安裝速度:
- **優化前**: 5-10 分鐘 (含地端模型下載)
- **優化後**: ~30 秒 (僅雲端 API)
- **速度提升**: **10-20 倍**

## 💡 注意事項

1. **首次使用本地模型**: 會自動下載模型文件,請確保網路暢通
2. **模型快取位置**: `~/.cache/huggingface/`
3. **切換 Provider**: 可在知識庫配置中隨時切換,無需重新安裝
