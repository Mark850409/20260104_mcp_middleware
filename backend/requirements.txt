# ============================================
# MCP Platform Backend - 核心依賴
# ============================================

# Web 框架
Flask==3.0.0
Flask-CORS==4.0.0

# HTTP 請求
requests==2.31.0

# 資料庫
PyMySQL==1.1.0
cryptography>=41.0.0

# 認證與安全
PyJWT==2.8.0
bcrypt==4.1.2
python-dotenv==1.0.0


# ============================================
# AI SDK - 雲端 API
# ============================================
openai>=1.0.0
google-generativeai>=0.3.0
anthropic>=0.8.0

# ============================================
# LINE Bot SDK
# ============================================
line-bot-sdk==3.8.0

# ============================================
# RAG 相關 - 核心套件
# ============================================
numpy==1.26.4
faiss-cpu==1.7.4
pypdf==3.17.4
python-docx==1.1.0
tiktoken==0.5.2

# ============================================
# RAG 相關 - 可選套件 (僅在使用本地 Embedding 時需要)
# ============================================
# 注意: sentence-transformers 會下載大型模型 (數百MB到數GB)
# 如果只使用雲端 Embedding API (OpenAI/Google),可以不安裝此套件
# 安裝方式: pip install sentence-transformers==2.2.2
# sentence-transformers==2.2.2

# ============================================
# 說明
# ============================================
# 快速安裝 (不含本地模型): pip install -r requirements.txt
# 完整安裝 (含本地模型): 取消註解 sentence-transformers 後再安裝
